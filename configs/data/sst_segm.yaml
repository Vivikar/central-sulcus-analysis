_target_: src.data.self_supervised_segm.ContrastiveDataModule

dataset_cfg:
  dataset: via11
  nviews: 2
  target: GM
  skull_strip: null #'half' # half
  resample: null # if None no resampling
  # - 1.5
  # - 1.5 
  # - 1.5  
  use_2x2x2_preproc: False
  padd2same_size: null # wether to pad the image to the same size False for via11
# CHANGE ALSO PADD TO DOUBLE THE SIZE?
train_batch_size: 3
validation_batch_size: 3
num_workers: 10