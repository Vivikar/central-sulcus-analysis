defaults:
  - model_checkpoint.yaml
  # - early_stopping.yaml
  # - model_summary.yaml
  - rich_progress_bar.yaml
  - device_stats.yaml
  - _self_

model_checkpoint:
  dirpath: ${paths.output_dir}/checkpoints
  filename: "epoch-{epoch:03d}-Esubj-{val/Esubj:.4f}"
  monitor: "val/dsc"
  mode: "max"
  save_top_k: 3
  save_last: True
  auto_insert_metric_name: False

early_stopping:
  monitor: "val/dsc"
  patience: 100
  mode: "max"

model_summary:
  max_depth: -1
